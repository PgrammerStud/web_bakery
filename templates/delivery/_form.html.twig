{{ form_start(form) }}

{{ form_row(form.orders) }}
{{ form_row(form.delivery_date) }}
{{ form_row(form.status) }}
{{ form_row(form.delivery_fee) }}

<button type="submit" class="btn btn-primary">Save</button>

{{ form_end(form) }}